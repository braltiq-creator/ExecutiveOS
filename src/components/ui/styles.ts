import { ds } from "@/design-system/tokens";

export const focusRing = ds.focusRing;

export const cardBase = ds.elevation.floating;

export const inputBase =
  "block w-full rounded-[var(--eos-radius-md)] border border-[var(--eos-color-divider)] bg-[var(--eos-color-surface)] px-[var(--eos-space-md)] py-[var(--eos-space-sm)] text-[length:var(--eos-type-body-size)] text-[var(--eos-color-text)] placeholder:text-[var(--eos-color-text-muted)] transition-[border-color] duration-[var(--eos-duration-fast)] hover:border-[var(--eos-border-strong)] focus-visible:border-[var(--eos-color-focus)] disabled:cursor-not-allowed disabled:opacity-60";

export const labelBase = ds.type.subheading;

export const mutedText = ds.type.supporting;

export const pageTitle = ds.type.displayXl;

export const sectionTitle = ds.type.heading;

export const overline = ds.type.label;
