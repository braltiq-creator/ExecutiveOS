"use client";

import { MissionControl } from "@/experience/mission-control";

/** Today — Executive Command Centre (Mission Control v3.0). */
export function TodayShell() {
  return <MissionControl />;
}
